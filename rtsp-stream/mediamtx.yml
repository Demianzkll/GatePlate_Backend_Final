paths:
  cam:
    runOnInit: ffmpeg -f v4l2 -input_format mjpeg -framerate 30 -video_size 1280x720 -i /dev/video0 -c:v libx264 -preset ultrafast -tune zerolatency -b:v 2M -rtsp_transport tcp -f rtsp rtsp://localhost:8554/cam
    runOnInitRestart: yes